## 编译
gradle jar
生成jar包在build/libs目录下

## 运行
将pick.bat、picker.properties拷贝到build/libs，运行pick.bat即可

## picker.properties配置

catalogUrl   小说的目录页
urlPrefix   各个章节url共同前缀
filePath  抓取后存储路径
shiftWord 屏蔽文章内的文字，多个用&分割
